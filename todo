- Gestion des livraisons
	- Base
		. Creer les tables:
			- livraisonChauffeur : id, nom
			- livraisonVoiture : id, nom
			- livraisonColis : id, nom, kg
			- livraisonStatut : id, valeur
			- livraisonLivraison : id, idColis, adresseDepart, adresseDestination, idStatut, idChauffeur, idVoiture, daty, salaire, montant, benefice
			- livraisonSociete : id, nom, chiffreAffaire, coutRevient
			- livraisonSocieteEntrepot : id, entrepot, idSociete
			- Equivalence : id, kg, montant

		. Inserer les données pour:
			- Chauffeur
			- Voiture
			- Colis
			- Statut

	- Design
		. Formulaire d insertion de livraison : 
			- dropdown des colis en attente
			- input text adresseDepart readonly : entrepot de societe
			- input text adresseDestination
			- dropdown de voiture
			- boutton Annuler
			- boutton Livrer

	- Fonction
		. ColisModel : getAll()
		. VoitureModel : getAll()
		. ChauffeurModel : getAll()
		. SocieteModel : getEntrepot(), update()
		. LivraisonModel : save(), update(), calculBenefice()

	- Integration
		. LivraisonController : 
			. doGet:
				. appel de ColisModel : getAll()
				. appel de VoitureModel : getAll()
				. appel de ChauffeurModel : getAll()
				. appel de SocieteModel : getEntrepot()
			. doPost:
				. Si boutton Annuler
					. appel de LivraisonModel : update()
				. Si boutton Livrer
					. appel de LivraisonModel : update() : statut -> livré, salaire -> vrai salaire, benefice -> appel de calculBenefice()
					. appel de SocieteModel : update() : chiffreAffaire -> + montant, coutRevient -> + salaire

- Page pour afficher benefice par periode
	- Base
		. utiliser :
			. livraisonLivraison

	- Design
		. Afficher un tableau pour voir les benefice par periode: chiffreAffaire, coutRevient, benefice, date

	- Fonction
		. LivraisonModel : getBilan() => group by date de livraison

	- Integration
		. Formulaire de saisie de date:
			. input jour
			. input mois
			. input annee
			. input submit rechercher
		. tableau pour affichage: chiffreAffaire, coutRevient, benefice, date